import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class SummaryYearView extends StatelessWidget {
  const SummaryYearView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Year summary content goes here'),
    );
  }
}
